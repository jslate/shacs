h1 Events

p To see more events, follow more #{link_to 'activities', activities_path}.

table
  tr
    th Activity
    th Host
    th Location
    th Date/time
    th Notes
  - @events.each do |event|
    tr
      td #{event.activity.name}
      td #{event.user.name}
      td #{event.location}
      td #{l(event.start_time, format: :kinda_short)}
      td #{event.notes}

.controls
  = link_to 'New Event', new_event_path, class: 'button'
